#pragma once

// Lokale GardenFlow-Zugangsdaten.
// Für einen reinen Display-/Offline-Test dürfen beide Werte leer bleiben.
// Für WLAN-Betrieb hier die eigenen Daten eintragen.
//
// Diese Datei sollte nicht mit echten Zugangsdaten in Git eingecheckt werden.

namespace AppSecrets
{
    static constexpr char WIFI_SSID[] = "";
    static constexpr char WIFI_PASSWORD[] = "";
}
